use dioxus::prelude::*;

use crate::toolset::StoryMetadata;

/*
    pub title: String,
    pub genre: String,
    pub themes: Vec<String>,
    pub target_audience: String,
    pub synopsis: String,
*/

#[component]
pub fn StoryRenderer(sm: StoryMetadata) -> Element {
    rsx! {}
}
